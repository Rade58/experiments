{
  "extends": "../../tsconfig.json",
  "include": [
    "src",
    "env.ts",
    "vitest.config.ts",
    "tests"
  ],
  "compilerOptions": {
    // no need these (we inherit them)
    // "noEmit": true,
    // "target": "ESNext",
    // "module": "NodeNext",
    //
    "noFallthroughCasesInSwitch": true,
    // 
    "rewriteRelativeImportExtensions": true,
    "erasableSyntaxOnly": true,
    "verbatimModuleSyntax": true,
    // this only works with moduleResolution: "bundler""
    // so not using it now, but keeping for future reference
    // "allowImportingTsExtensions": true,
    "types": [
      "vitest/globals",
      "node",
    ]
  }
}