// import { verifyToken } from '../src/utils/auth/jwt.ts'
// import env from '../src/env.ts'

describe.skip('Authentication util functions', () => {
	// console.log('Using JWT_SECRET:', env.JWT_SECRET)
	// console.log('Using DATABASE_URL:', env.DATABASE_URL)

	it('should work', async () => {
		const token = ''
		// const data = await verifyToken(token)
	})
})
